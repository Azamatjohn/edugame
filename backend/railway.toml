[build]
builder = "nixpacks"

[deploy]
startCommand = "gunicorn edugame_backend.wsgi --bind 0.0.0.0:$PORT"
healthcheckPath = "/api/leaderboard/"
releaseCommand = "python manage.py migrate --noinput"

